window.GAMEDAY_CONFIG = {
  // Có thể để trống. Khi mở trên GitHub Pages, game sẽ hỏi URL Cloudflare Tunnel.
  // Nếu dùng một tunnel cố định, điền URL tại đây, ví dụ:
  // API_BASE: 'https://game-api.example.com'
  API_BASE: ''
};
